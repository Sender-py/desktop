name: Build Sender.py

on:
  push:
    branches: [main]
  release:
    types: [created]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyinstaller customtkinter requests
      - run: pyinstaller --noconsole --onefile src/sender.py
      - uses: actions/upload-artifact@v4
        with:
          name: sender-windows
          path: dist/sender.exe

  build-linux-deb:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyinstaller customtkinter requests
      - run: pyinstaller --onefile src/sender.py
      - run: |
          sudo apt-get install -y ruby ruby-dev
          sudo gem install fpm
          fpm -s dir -t deb -n sender-py -v 1.0 dist/sender=/usr/local/bin/sender
      - uses: actions/upload-artifact@v4
        with:
          name: sender-linux-deb
          path: "*.deb"

  build-linux-rpm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyinstaller customtkinter requests
      - run: pyinstaller --onefile src/sender.py
      - run: |
          sudo apt-get install -y ruby ruby-dev rpm
          sudo gem install fpm
          fpm -s dir -t rpm -n sender-py -v 1.0 dist/sender=/usr/local/bin/sender
      - uses: actions/upload-artifact@v4
        with:
          name: sender-linux-rpm
          path: "*.rpm"

build-linux-pkg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyinstaller customtkinter requests
      - run: pyinstaller --onefile src/sender.py
      - run: |
          sudo apt-get install -y ruby ruby-dev
          sudo gem install fpm
          fpm -s dir -t pacman -n sender-py -v 1.0 dist/sender=/usr/local/bin/sender
      - uses: actions/upload-artifact@v4
        with:
          name: sender-linux-pkg
          path: "*.pkg.tar.zst"